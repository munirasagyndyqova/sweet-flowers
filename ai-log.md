# AI Usage Log Sweet Flowers

Per the course AI policy, AI was used only to explain concepts, organize work and check structure never to write our page text, report, or images. All site text, data and photos are our own.

| Date | Student | Question asked to AI | How the answer was used |
|---|---|---|---|
| 10 September | Sagyndykova Munira | Asked how to split the 8 required pages fairly between 3 students so each person's workload is roughly equal | Used the suggested split as a starting point, then adjusted it ourselves based on who wanted which pages |
| 11 September | Kenes Nurziya | Asked for a few synonyms/alternative phrasings for a sentence we had already written about delivery zones, to make it read more naturally | Picked one option and adapted it into our own wording |
| 11 September | Koshkinbayeva Akbota | Asked how to properly cite/attribute a real customer quote in HTML (which tag: q, blockquote, cite) | Used this to pick `<blockquote>` + `<cite>` correctly in reviews.html |
| 13 September | Koshkinbayeva Akbota | Asked what makes a good structure for a README file | Used the explanation to decide what sections our README should have; wrote the content ourselves |
| 17 September | Sagyndykova Munira | "How do I create a beautiful neon text glow effect for an h1 tag using text-shadow and @keyframes?"| Used the suggested `text-shadow` values and `@keyframes glow` animation structure in `index.css` to create the animated pink neon glow effect on the main `h1` heading |
| 17 September | Kenes Nurziya |"How to style a fixed quick order button at the bottom-right corner of the screen using position: fixed?"| Applied `position: fixed` with `bottom: 20px` and `right: 20px` to keep the `.quick-order` button pinned to the screen corner during scrolling |
| 18 September | Koshkinbayeva Akbota | "How do I align action buttons horizontally in a wrap row using Flexbox justify-content and gap?"| Configured `.review-actions` with `display: flex`, `flex-wrap: wrap`, `justify-content: center`, and `gap: 1rem` for aligned, responsive button layouts |
| 18 September | Sagyndykova Munira | "Which positioning properties should I use to pin a 'Popular' badge overlay to the top-right corner of a product card image?"| Set `position: relative` on `.flower-card` and `position: absolute` with `top: 12px` and `right: 12px` on `.badge-popular` to overlay the badge correctly |
